/* Entry point of `vite.styles.config.js` when building app styles for distribution.
   Used to combine local app styles with distributed lib styles so consumers don't
   have to import two stylesheets. */

import '@h5web/lib/styles.css';
